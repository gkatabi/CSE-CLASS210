using System;

class GetFractionString
{
    static void Main(string[] args)
    {
        int  U , L, div, decimal
        Console.WriteLine(" Enter two numbers you would like to divide.first to be divided and the last to be dividable"); 
        Console.WriteLine( {0} "/" {1}, U,L)
        U = int.perse( Console.ReadLine());
        L = int.perse(Console.ReadLine());
        string Fraction = Console.ReadLine(int {0}, "/" {1} U,L);
        Console.Write($"{Fraction}");

      
    }
}
